import Logger from '../logger/Logger';
import BaseConnectionHealthPolicy from './BaseConnectionHealthPolicy';
import ConnectionHealthData from './ConnectionHealthData';
import ConnectionHealthPolicy from './ConnectionHealthPolicy';
import ConnectionHealthPolicyConfiguration from './ConnectionHealthPolicyConfiguration';
export default class SendingAudioFailureConnectionHealthPolicy extends BaseConnectionHealthPolicy implements ConnectionHealthPolicy {
    private logger;
    private readonly sendingAudioFailureSamplesToConsider;
    private readonly sendingAudioFailureInitialWaitTimeMs;
    private readonly coolDownTimeMs;
    private readonly maximumTimesToWarn;
    private warnCount;
    private lastWarnTimestampMs;
    constructor(logger: Logger, configuration: ConnectionHealthPolicyConfiguration, data: ConnectionHealthData);
    private isSendingAudioUnhealthy;
    health(): number;
}
