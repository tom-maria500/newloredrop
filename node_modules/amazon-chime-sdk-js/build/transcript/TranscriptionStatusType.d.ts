declare enum TranscriptionStatusType {
    STARTED = "started",
    INTERRUPTED = "interrupted",
    RESUMED = "resumed",
    STOPPED = "stopped",
    FAILED = "failed"
}
export default TranscriptionStatusType;
